
// // const bodyMargin = (theme) => ({
// //     marginLeft: theme.mixins.drawer.width, // Use the width of the drawer defined in theme.mixins.drawer
// //     transition: theme.transitions.create('margin', {
// //         easing: theme.transitions.easing.sharp,
// //         duration: theme.transitions.duration.enteringScreen,
// //     }),
// //     overflowX: 'hidden',
// // });

// const bodyMargin = styled('div')(({ theme }) => ({
//     marginLeft: theme.mixins.drawer.width, // Use the width of the drawer defined in theme.mixins.drawer
//     transition: theme.transitions.create('margin', {
//         easing: theme.transitions.easing.sharp,
//         duration: theme.transitions.duration.enteringScreen,
//     }),
//     overflowX: 'hidden',
// }));

// export { bodyMargin }